# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                           controllers.BlackjackController.start()

GET		/blackjack/:command			controllers.BlackjackController.blackjack(command: String)

GET		/blackjack					controllers.BlackjackController.index()

GET     /rules                		controllers.BlackjackController.rules()

GET     /json                       controllers.BlackjackController.json()

POST	/json/:command				controllers.BlackjackController.jsonCommand(command: String)

POST	/newPlayer/:playername		controllers.BlackjackController.addNewPlayer(playername: String)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)